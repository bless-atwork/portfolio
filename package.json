{
  "name": "portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^15.29.0",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "gsap": "^3.15.0"
  }
}
